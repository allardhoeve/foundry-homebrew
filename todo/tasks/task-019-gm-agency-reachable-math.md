# Task 019: getReachableRange — cancelled

## Decision

Cancelled. The penalty already tells the consumer how many entries to dim from the top — a dedicated function just restates arithmetic the caller can do inline. The results panel will dim entries directly using the penalty value.
